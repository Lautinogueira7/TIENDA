import logo from '../cart.png';

const CartWidget = () => {

    return (
        <div>
            <a href="$">
                <img className='carro' src={logo} alt=""></img>;
            </a>
        </div>
    )
}

export default CartWidget